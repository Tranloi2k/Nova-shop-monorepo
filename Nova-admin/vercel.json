{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd .. && corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm install --frozen-lockfile --filter nova-admin-react...",
  "buildCommand": "pnpm build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
